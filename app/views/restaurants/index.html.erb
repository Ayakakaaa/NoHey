
<header class="header-restaurant">
  <% if can? :crud, @restaurant %>
    <%= link_to 'New Restaurant', new_restaurant_path, class: "new-restaurant" %>
  <% end %>

  <div class="search">
    <input type="text" name="search" spacefolder ="What Would you like to have today?" />
    <input type="submit" value="search">
    <div>Tags</div>
  </div>
</header>


<article class = "restaurant-container">
  <% @restaurants.each do |restaurant| %>
    <div class = "restaurant">
      <h3 class="title"> <%= restaurant.name %></h3> 
        <%= image_tag(restaurant.images.last, class: "img-fit pic-restaurant") if restaurant.images.attached? %>
       

      <div class="info">
        <div>
          <small style="font-weight: lighter;">City:</small>
          <small><%= restaurant.city %></small>
        </div>

        <div>
         <small style="font-weight: lighter;">Website: </small>
          <p><%=link_to restaurant.website, restaurant.website %></p>
        </div>

        <p><%= link_to 'Interested', restaurant %></p>
        <div>
          <% if can? :crud, @restaurant %>
            <p><%= link_to 'Edit', edit_restaurant_path(restaurant) %></p>
            <p><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' } %></p>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</article>

